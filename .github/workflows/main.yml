name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Check app and dependencies
        uses: msfjarvis/setup-android@1.0
        with:
          entrypoint: ./gradlew
          args: bundleDebug assembleDebug
