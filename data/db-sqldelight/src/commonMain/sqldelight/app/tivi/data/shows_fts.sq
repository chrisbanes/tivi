CREATE VIRTUAL TABLE IF NOT EXISTS `shows_fts` USING FTS4(
    `title` TEXT,
    `original_title` TEXT,
    content=`shows`
);

-- queries

search:
SELECT shows.* FROM shows
INNER JOIN shows_fts AS fts ON shows.id = fts.docid
WHERE fts.title MATCH :filter;
